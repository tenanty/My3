<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="channelsoft.com.my3" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChangeActivity2" android:label="第二个activity正在展示" ></activity>
        <activity android:name=".HelloWorldActivity" android:label="HelloWorld展示"></activity>
        <activity android:name=".AudioActivity" android:label="录音展示"></activity>
        <activity android:name=".AlertActivity" android:label="弹出信息"></activity>
        <activity android:name=".UploadActivity" android:label="文件上传"></activity>
        <activity android:name=".OpenActivity" android:label="url打开"></activity>
        <activity android:name=".LocationActivity" android:label="地理位置"></activity>
        <activity android:name=".MainPageActivity" android:label="翻页效果"></activity>
    </application>


    <!-- 联网权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 地理位置信息 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>
